# configured via ansible
TYPE="Ethernet"
BOOTPROTO="static"
DNS1="{{ ocp_dns_1 }}"
DNS2="{{ ocp_dns_2 }}"
GATEWAY="{{ ocp_gateway }}"
HOSTNAME="{{ ansible_hostname }}"
IPADDR="{{ ansible_host }}"
NETMASK="{{ ansible_default_ipv4.netmask }}"
NM_CONTROLLED="yes"
IPV6INIT="no"
NAME="enp1s0"
DEVICE="enp1s0"
ONBOOT="yes"
